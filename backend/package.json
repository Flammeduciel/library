{
  "name": "akieni-bibliotheque-backend",
  "version": "1.0.0",
  "description": "API Express de la bibliotheque de quartier (image Docker autonome)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "psql -U postgres -d bibliotheque -f db/schema_prod.sql",
    "db:seed": "psql -U postgres -d bibliotheque -f db/seed_demo.sql",
    "db:reset": "psql -U postgres -c \"DROP DATABASE IF EXISTS bibliotheque\" -c \"CREATE DATABASE bibliotheque\" && npm run db:migrate && npm run db:seed"
  },
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.13.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}